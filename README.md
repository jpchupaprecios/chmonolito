sudo apt update
sudo apt install chromium-chromedriver


si falla correr estos comandos y pasarlos a chatgpt

chromedriver --version

administrador.google-chrome --version

administrador.which google-chrome


al arrancar algun error puede necesitar esto
sudo apt-get install xvfb
